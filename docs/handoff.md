# Текущее состояние проекта

> Обновлять при каждом значимом изменении. Здесь только срез "сейчас", не
> история — история в `docs/records/`, пожелания владельца — в
> `docs/backlog.md`.

## Релиз

- `flutter_team_logger`: **0.7.1 готова, но не опубликована**. Версия в
  `pubspec.yaml` и запись в `CHANGELOG.md` есть; коммита `Release 0.7.1`,
  тега и публикации нет. Публикация требует отдельного разрешения владельца
  (см. `AGENTS.md`).
- Последняя опубликованная версия — 0.7.0 (тег `v0.7.0`).
- Зависимость `team_logger`: `^0.7.1` (последняя опубликованная — 0.7.1).

## Документация

- `README.md`/`README.ru.md` заведены и синхронны, в обоих есть раздел про
  экран деталей с предупреждением про `Loggable.sanitizer`. Обновление до
  team_logger 0.7.1 их не затронуло: снятый обход геттера был внутренним.
- В `lib/` комментарии полностью на английском.

## Состояние дерева

- Рабочее дерево чистое. `main` опережает `origin/main` на 17 коммитов — не
  запушено.
- Сборка `example` под macOS на Flutter 3.47 переписывает `example/macos/`:
  плагины (`window_manager`, `screen_retriever_macos`) переезжают с
  CocoaPods на SwiftPM, а `Podfile` поднимает целевую платформу с 10.14 до
  12.0. Правки затрагивают `Podfile`, `Podfile.lock`, `project.pbxproj` и
  `Runner.xcscheme`. Агент их откатывает после проверки: поднять целевую
  версию macOS — решение владельца, а не побочный эффект прогона сборки.

## Активная работа

Активной работы нет — дерево в готовом состоянии, ждёт решений владельца.

Последнее сделанное: обновление `team_logger` до 0.7.1 и подготовка к
публикации 0.7.1, отчёт —
`docs/records/2026-08-24[8]-team-logger-0.7.1-upgrade-report.md`. Заодно
исправлен дефект `.pubignore`: файл не дополняет корневой `.gitignore`, а
отменяет его целиком, из-за чего в pub-архив уезжали 96 МБ кэша сборки
(30 МБ архива против 298 КБ). Опубликованная 0.7.0 не пострадала — кэша в
дереве на тот момент не было.

До этого — экран деталей лога `LogDetails`. Спека и план —
`docs/records/2026-08-24[5]-log-details-design.md` и
`2026-08-24[6]-log-details-plan.md`, отчёт —
`2026-08-24[7]-log-details-report.md`.

Ждёт решения владельца:

- визуальная проверка экрана деталей вживую
  (`cd example && flutter run -d macos`) — агент проверил поведение
  widget-тестом и сборку, но не внешний вид;
- публикация 0.7.1.

К публикации всё готово: дерево чистое, `dart pub publish --dry-run` даёт
0 предупреждений. Не хватает только разрешения владельца — отдельным
запросом, состоящим из одной просьбы опубликовать (см. `AGENTS.md`).

## Проверка

- `dart analyze` (корень и `example/`) — чисто.
- `flutter test` — 15 тестов, зелёные: 13 на модель дерева
  (`test/log_node_test.dart`) и 2 smoke-теста экрана
  (`test/log_details_test.dart`).
- `example` собирается под macOS.
- `dart pub publish --dry-run` — архив 298 КБ, лишнего в нём нет.
