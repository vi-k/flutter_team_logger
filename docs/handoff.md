# Текущее состояние проекта

> Обновлять при каждом значимом изменении. Здесь только срез "сейчас", не
> история — история в `docs/records/`, пожелания владельца — в
> `docs/backlog.md`.

## Релиз

- `flutter_team_logger`: рабочая версия в дереве — 0.7.0, ещё не
  опубликована на pub.dev (последняя опубликованная — 0.5.0).
- Зависимость `team_logger`: `^0.7.0` (последняя опубликованная —
  0.7.0).
- Ветка `main` на 2 коммита впереди `origin/main` (не запушено).

## Документация

- `README.md`/`README.ru.md` заведены (проект на ранней стадии, README
  минимальный). Перевод держать синхронным с оригиналом при каждой правке
  README — см. `AGENTS.md`.
- В `lib/` комментарии полностью на английском (переведены последние
  русскоязычные doc-комментарии в `logs.dart`/`filter.dart`).

## Активная работа

Нет активной работы в коде. team_logger только что обновлён с 0.5.1 до
0.7.0 (с миграцией под breaking changes) — подробности в `docs/records/`.

Следующий шаг не определён — смотри `docs/backlog.md`. Публикация 0.7.0 на
pub.dev не запрошена владельцем отдельно — см. `AGENTS.md`.

## Проверка

- Тестов в репозитории нет.
- `dart analyze` (корень и `example/`) — чисто.
