# Текущее состояние проекта

> Обновлять при каждом значимом изменении. Здесь только срез "сейчас", не
> история — история в `docs/records/`, пожелания владельца — в
> `docs/backlog.md`.

## Релиз

- `flutter_team_logger`: **0.7.1 готова, но не опубликована**. Версия в
  `pubspec.yaml` и запись в `CHANGELOG.md` есть; коммита `Release 0.7.1`,
  тега и публикации нет. Публикация требует отдельного разрешения владельца
  (см. `AGENTS.md`).
- Последняя опубликованная версия — 0.7.0 (тег `v0.7.0`).
- Зависимость `team_logger`: `^0.7.0` (последняя опубликованная — 0.7.0).

## Документация

- `README.md`/`README.ru.md` заведены и синхронны, в обоих есть раздел про
  экран деталей с предупреждением про `Loggable.sanitizer`.
- В `lib/` комментарии полностью на английском.

## Состояние дерева

- `main` опережает `origin/main` на 11 коммитов — не запушено.
- В дереве незакоммичены `analysis_options.yaml` и
  `example/analysis_options.yaml` (в обоих добавлены платформенные папки в
  `analyzer.exclude`). Это правки владельца, появившиеся во время работы над
  экраном деталей; агент их не трогал. Расследовать не нужно — спросить
  владельца, коммитить ли.

## Активная работа

Сделан экран деталей лога `LogDetails` (пункт из `docs/backlog.md` про
полноценный вывод информации по конкретному логу). Спека и план —
`docs/records/2026-08-24[5]-log-details-design.md` и
`2026-08-24[6]-log-details-plan.md`, отчёт —
`2026-08-24[7]-log-details-report.md`.

Не сделано и ждёт решения владельца:

- визуальная проверка экрана вживую (`cd example && flutter run -d macos`) —
  агент проверил поведение widget-тестом и сборку, но не внешний вид;
- публикация 0.7.1.

## Проверка

- `dart analyze` (корень и `example/`) — чисто.
- `flutter test` — 15 тестов, зелёные: 13 на модель дерева
  (`test/log_node_test.dart`) и 2 smoke-теста экрана
  (`test/log_details_test.dart`).
- `example` собирается под macOS.
